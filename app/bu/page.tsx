"use client"

import React from 'react'
// import WithSkeleton from '../components/SkeletonLoader'
// import WithSuspenseSkeleton from '../components/SkeletonLoader'

function Page() {
 

  return (
    <div className='flex flex-col gap-4'>
      {/* <WithSkeleton isLoading={isLoading} height={24}>
        <h1 className='text-2xl font-bold'>This is the title</h1>
      </WithSkeleton>

      <WithSkeleton isLoading={isLoading} height={18}>
        <p>This is paragraph one.</p>
      </WithSkeleton>

      <WithSkeleton isLoading={isLoading} height={18}>
        <p>This is paragraph two.</p>
      </WithSkeleton> */}
    </div>
  )
}

export default Page
